export const VAT_RATE = 0.2;

export const PROMO_CODES = {
    WELCOME_OFFER: 'B1M20',
};

export const PROMO_RULES = {
    WELCOME_OFFER_DISCOUNT: 0.5,
};

// Default fallback year if none provided (Consider using dynamic current year in logic instead)
export const DEFAULT_FALLBACK_YEAR = 2026;
